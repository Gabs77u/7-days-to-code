const nome = prompt ("Me diga seu nome, por favor:");
alert (`Olá,${nome}`);
//Interação de Usuario 1
const FrontendOrBackend = prompt(`${nome} ,deseja seguir para a area de Front-End ou Back-End ?`);
alert (`Certo`)
if (FrontendOrBackend === "Front-End"||"Frontend"||"frontend"||"front-end"){
    const ReactOrVue = prompt("Deseja seguir para a area de React ou Vue ?");
    if(ReactOrVue === "React"||"react"||"REACT"){
    }
}